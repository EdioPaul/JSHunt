import React from 'react';

import './styles.css';

//formato sem estado de classe, retorna função direto sem necessidade do return.
// class extends utiliza-se para componentes com mudança de estado.
//abaixo está minificada (Stateless component)
const Header = () => <header id="main-header">JSHunt</header>

export default Header;
